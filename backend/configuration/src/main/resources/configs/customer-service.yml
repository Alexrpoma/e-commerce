spring:
  application:
    name: customer
  datasource:
    url: jdbc:postgresql://localhost:8000/customer
    driver-class-name: org.postgresql.Driver
    username: admin
    password: adm_psql
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka

server:
  port: 9000